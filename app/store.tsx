import { StoreScreen } from "../src/views/StoreScreen";

/**
 * Expo Router entry point for the store route
 * This file should remain thin - delegate UI to views and logic to controllers
 */
export default function Store() {
  return <StoreScreen />;
}

