import { StoreOwnerSettingsView } from "../../src/views/StoreOwnerSettingsView";

/**
 * Expo Router entry point for the store owner settings route
 * This file should remain thin - delegate UI to views and logic to controllers
 */
export default function StoreOwnerSettings() {
  return <StoreOwnerSettingsView />;
}

